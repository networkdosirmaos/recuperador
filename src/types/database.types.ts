export type Json = string | number | boolean | null | { [key: string]: Json | undefined } | Json[]; export interface Database { public: { Tables: { leads: { Row: { id: string; created_at: string; updated_at: string; name: string; email: string | null; phone: string | null; customer_id: string | null; product_name: string | null; gateway: string | null; gateway_status: string | null; gateway_event: string | null; gateway_updated_at: string | null; gateway_metadata: Json | null; refunded_at: string | null; chargedback_at: string | null; refund_reason: string | null; payment_method: string | null; reason: string | null; temperature: string; status: string; current_assignee_id: string | null; list_id: string | null; next_action_at: string | null; notes: string | null; history_log: Json | null; profiles?: { full_name: string } | null; lead_lists?: { name: string; type: string } | null; } }; profiles: { Row: { id: string; email: string; full_name: string; role: string; is_active: boolean; last_assigned_at: string | null; } } } } }; export type LeadRow = Database['public']['Tables']['leads']['Row']; export type ProfileRow = Database['public']['Tables']['profiles']['Row'];
